#include <stdio.h>
/**
 * main - A program that prints all posible combinations of single-digit numbers.
 *
 * Return: Always 0 (Success)
 */
int mai(void)
{
int num;
for (num = 0; num <=9; num++)
{
printf("%d", num);
}
printf("\n");
return 0;
}
