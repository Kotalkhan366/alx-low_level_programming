#include "main.h"

/**
 * main - A program that prints a straight line in the terminal
 *
 * Return: Always 0 Success.
 */

int main(void)
{
int lineLength = 20;
for (int i = 0; i < linelength; ++i)
{
printf("-");
}
printf("\n");
return (0);
}
