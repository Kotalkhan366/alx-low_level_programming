#include "main.h"
/**
 * main - A program that prints a square
 *
 * Return: Always 0 Success.
 */

int main(void)
{
int sidelength = 5;
for (int i = 0; i < sidelength; ++i)
{
for (int j = 0; j < sideLength; ++j)
{
printf("# ");
}
printf("\n");
}
return (0);
}
