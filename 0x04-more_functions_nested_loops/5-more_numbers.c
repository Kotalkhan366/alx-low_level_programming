#include "main.h"

/**
 * main - A program that prints 10 times the number from 0 to 14
 *
 * Return: Always 0 Success.
 */

int main(void)
{
for (int repeat = 0; repeat < 10; ++repeat)
{
for (int i = 0; i <= 14; ++i)
{
printf("%d\n", i);
}
}
return (0);
}
